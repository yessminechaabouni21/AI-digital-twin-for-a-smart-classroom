"""Domain models: framework-free entities shared across the whole system.

These are the vocabulary every other layer (twin engine, analytics,
agents, API) speaks. No I/O and no persistence concerns belong here.
"""
