"""Endpoints exposing descriptive/predictive analytics results."""

from fastapi import APIRouter

router = APIRouter(prefix="/analytics", tags=["analytics"])

# TODO: implement endpoints once analytics/ has working models.
