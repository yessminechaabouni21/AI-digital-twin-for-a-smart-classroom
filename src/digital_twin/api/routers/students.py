"""Endpoints for student resources (CRUD, twin-state lookups)."""

from fastapi import APIRouter

router = APIRouter(prefix="/students", tags=["students"])

# TODO: implement endpoints once StudentRepository exists.
